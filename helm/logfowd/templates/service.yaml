apiVersion: v1
kind: Service
metadata:
  name: {{ include "logfowd.fullname" . }}
  labels:
    {{- include "logfowd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  clusterIP: None
  selector:
    {{- include "logfowd.selectorLabels" . | nindent 4 }}
